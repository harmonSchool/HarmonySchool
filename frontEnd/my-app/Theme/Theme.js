import { useState } from "react";
const lightTheme = {
    backgroundColor: 'white',
    textColor: '#02092B',
    borderColor:"black"
  };
  
  const darkTheme = {
    backgroundColor: '#000000',
    textColor: 'white',
    borderColor:"white"
  };
  
  export { lightTheme, darkTheme };